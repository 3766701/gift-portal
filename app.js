const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);
const apiPrefix=window.location.pathname==='/gift'||window.location.pathname.startsWith('/gift/')?'/gift':'';
const api=path=>apiPrefix+path;

// Keep every activation-code field in sync with the target page's plain text input behavior.
const activationFields=$$('#code,#global-code,#qr-code,#order-id');
const key=new URLSearchParams(window.location.search).get('key');
if(key) activationFields.forEach(field=>{field.value=key;});

const activateTab=tab=>{$$('.layui-tab-title li').forEach(x=>x.classList.toggle('layui-this',x.dataset.tab===tab));$$('.layui-tab-item').forEach(x=>x.classList.toggle('layui-show',x.dataset.panel===tab));};
$('#tabs').addEventListener('click',e=>{const li=e.target.closest('li');if(!li||li.classList.contains('tab-disabled'))return;activateTab(li.dataset.tab)});
const setTabEnabled=(tab,enabled)=>{const item=$(`[data-tab="${tab}"]`);item.classList.toggle('tab-disabled',!enabled);item.setAttribute('aria-disabled',String(!enabled));item.tabIndex=enabled?0:-1;};
fetch(api('/api/config')).then(r=>r.json()).then(config=>{const features=config.features||{};setTabEnabled('global',features.global);setTabEnabled('steam',features.steam);setTabEnabled('qr',features.qr);if($('.layui-tab-title .layui-this').classList.contains('tab-disabled')){const next=['global','steam','qr'].find(tab=>features[tab]);if(next)activateTab(next);}}).catch(()=>{});
const setRedemptionLoading=(active,message='正在提货...')=>{$('#global-loading-message').textContent=message;$('#global-loading').hidden=!active;$$('#global-redeem-form button[type="submit"],#redeem-form button[type="submit"],#steam-guard-form button[type="submit"]').forEach(button=>button.disabled=active);};
const isEmail=value=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);
const closeResultModal=()=>{$('#result-modal').hidden=true;};
const showResultModal=message=>{$('#result-modal-message').textContent=message;$('#result-modal').hidden=false;$('#result-modal-confirm').focus();};
const closeSteamQrModal=()=>{$('#steam-qr-modal').hidden=true;};
const showSteamQrModal=(svg,message='请使用 Steam 手机客户端扫码并确认。')=>{const box=$('#steam-qr-modal-box');box.replaceChildren();const image=document.createElement('img');image.alt='Steam 扫码登录二维码';image.src=`data:image/svg+xml;charset=utf-8,${encodeURIComponent(svg)}`;box.append(image);$('#steam-qr-modal-message').textContent=message;$('#steam-qr-modal').hidden=false;};
const setSteamQrMessage=message=>{$('#steam-qr-modal-message').textContent=message;};
const responseMessage=async response=>{
  try{const data=await response.json();if(data.message)return data.message;}catch(error){}
  if(response.status===504)return '服务器处理超时，请稍后使用激活码查询提货状态。';
  return response.ok?'服务器响应异常，请稍后重试。':`请求失败（HTTP ${response.status}），请稍后重试。`;
};
const showCompletedQueryResult=(code,reward)=>{const message=$('#result-modal-message'),product=document.createElement('span');product.className='query-reward';product.textContent=reward;message.replaceChildren(document.createTextNode(`激活码:${code}/ 商品名称:`),product,document.createTextNode(' 已完成，请及时登录游戏查看游戏库存到账情况感谢您的购买!'));$('#result-modal').hidden=false;$('#result-modal-confirm').focus();};
const clearQueryHistory=()=>{$('#order-result').textContent='';$('#history-row').style.display='none';$('#history').replaceChildren();};
const showQueryHistory=details=>{const used=details.status==='已领取',table=$('#history'),headers=['序号','激活码','商品','兑换成功时间'],head=document.createElement('thead'),headRow=document.createElement('tr'),body=document.createElement('tbody'),row=document.createElement('tr');headers.forEach(label=>{const cell=document.createElement('th');cell.textContent=label;headRow.append(cell)});head.append(headRow);['1',details.code,details.reward,details.used_at||'--'].forEach(value=>{const cell=document.createElement('td');cell.textContent=value;row.append(cell)});body.append(row);table.replaceChildren(head,body);$('#history-row').style.display='block';$('#order-result').textContent=used?`激活码:${details.code}/ 商品名称:${details.reward} 已完成，请及时登录游戏查看游戏库存到账情况感谢您的购买!`:details.message;};
const queryActivationCode=async(code,{showHistory=false}={})=>{if(!code){if(showHistory)clearQueryHistory();showResultModal('请输入激活码。');return}try{const r=await fetch(api('/api/orders/'+encodeURIComponent(code))),d=await r.json();if(r.status===404){if(showHistory)clearQueryHistory();showResultModal('对不起，查询不到该激活码！');return}if(showHistory)showQueryHistory(d);if(d.status==='未领取'){showResultModal('该激活码还未提货，请先提交后，再查询结果哦!');return}if(d.status==='已领取'){showCompletedQueryResult(d.code||code,d.reward);return}showResultModal(d.message||'查询失败，请稍后重试。')}catch{if(showHistory)clearQueryHistory();showResultModal('查询失败，请确认后端已启动。')}};
$('#result-modal-close').addEventListener('click',closeResultModal);$('#result-modal-confirm').addEventListener('click',closeResultModal);$('#result-modal').addEventListener('click',e=>{if(e.target===e.currentTarget)closeResultModal();});
$('#global-redeem-form').addEventListener('submit',async e=>{e.preventDefault();const msg=$('#global-form-message'),code=$('#global-code').value.trim(),username=$('#global-user').value.trim(),password=$('#global-password').value;if(code.length<12||!username||!password){msg.textContent='请填写完整的激活码、全球账号和密码。';return}if(!isEmail(username)){msg.textContent='请输入正确的邮箱格式。';return}msg.textContent='';setRedemptionLoading(true);try{const r=await fetch(api('/api/redeem/global'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code,username,password})});const data=await r.json().catch(()=>({}));if(data.fallback_to_steam){$('#code').value=code;activateTab('steam');showResultModal(data.message||'全球提货暂时不可用，请改用 Steam 提货。');return}showResultModal(data.message||await responseMessage(r));}catch{showResultModal('无法连接提货服务，请稍后重试。')}finally{setRedemptionLoading(false);}});$('#check-global-order').addEventListener('click',()=>{queryActivationCode($('#global-code').value.trim())});
let pendingSteamRedemption=null,pendingSteamGuardType='';
const configureSteamGuard=({type,message}={})=>{const email=type==='email';pendingSteamGuardType=email?'email':'';$('#steam-guard-title').textContent=email?'Steam 邮箱验证码验证':'Steam 令牌验证';$('#steam-token').placeholder=email?'请输入 Steam 邮箱验证码':'5 位令牌或 7 位备用码';$('#steam-guard-message').textContent=message||(email?'Steam 已向该账号绑定邮箱发送验证码，请查收后输入。':'请输入 Steam 手机令牌或备用码。');};
const closeSteamGuard=()=>{pendingSteamRedemption=null;pendingSteamGuardType='';$('#steam-token').value='';$('#steam-guard-message').textContent='';$('#steam-guard-modal').hidden=true;};
const submitSteamRedemption=async(payload,{fromGuard=false}={})=>{const msg=$('#form-message');msg.textContent='';if(fromGuard)$('#steam-guard-modal').hidden=true;setRedemptionLoading(true,fromGuard?(payload.steam_guard_type==='email'?'正在验证邮箱验证码并提货...':'正在验证Steam令牌并提货...'):'正在提货...');try{const r=await fetch(api('/api/redeem'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const data=await r.json().catch(()=>({}));if(data.steam_guard_required){pendingSteamRedemption={code:payload.code,steam_user:payload.steam_user,steam_password:payload.steam_password};$('#steam-token').value='';configureSteamGuard({type:data.steam_guard_type,message:data.message});$('#steam-guard-modal').hidden=false;$('#steam-token').focus();return;}if(r.ok){$('#steam-password').value='';closeSteamGuard();}showResultModal(data.message||`请求失败（HTTP ${r.status}），请稍后重试。`);}catch{showResultModal('无法连接提货服务，请稍后重试。')}finally{setRedemptionLoading(false);}};
$('#redeem-form').addEventListener('submit',async e=>{e.preventDefault();const code=$('#code').value.trim(),username=$('#steam-user').value.trim(),password=$('#steam-password').value;if(code.length<12||!username||!password){$('#form-message').textContent='请输入完整的激活码、Steam 账号和密码。';return}pendingSteamRedemption={code,steam_user:username,steam_password:password};await submitSteamRedemption(pendingSteamRedemption);});
$('#steam-guard-form').addEventListener('submit',async e=>{e.preventDefault();const token=$('#steam-token').value.trim();if(!pendingSteamRedemption||!token){$('#steam-guard-message').textContent=pendingSteamGuardType==='email'?'请输入 Steam 邮箱验证码。':'请输入Steam令牌或备用码。';return}await submitSteamRedemption({...pendingSteamRedemption,steam_token:token,steam_guard_type:pendingSteamGuardType},{fromGuard:true});});
$('#steam-guard-close').addEventListener('click',closeSteamGuard);
$('#check-order-short').addEventListener('click',()=>{queryActivationCode($('#code').value.trim())});
$('#query-order').addEventListener('click',()=>{queryActivationCode($('#order-id').value.trim(),{showHistory:true})});
let steamQrId=null,steamQrTimer=null,steamQrInFlight=false,steamQrCloseTimer=null,steamQrClaimingShownAt=0;
const stopSteamQrPolling=()=>{if(steamQrTimer){clearInterval(steamQrTimer);steamQrTimer=null;}};
const clearSteamQrCloseTimer=()=>{if(steamQrCloseTimer){clearTimeout(steamQrCloseTimer);steamQrCloseTimer=null;}};
const failClosedSteamQr=()=>{if(!steamQrId)return;stopSteamQrPolling();steamQrId=null;setRedemptionLoading(false);showResultModal('二维码关闭后 30 秒内未完成 Steam 登录。');};
const closeSteamQrWithGrace=()=>{closeSteamQrModal();if(!steamQrId)return;clearSteamQrCloseTimer();setSteamQrMessage('二维码已关闭，30 秒内完成扫码可继续提货。');steamQrCloseTimer=setTimeout(failClosedSteamQr,30000);};
const ensureSteamQrClaimingVisible=async()=>{if(!steamQrClaimingShownAt)return;const remaining=1200-(Date.now()-steamQrClaimingShownAt);if(remaining>0)await new Promise(resolve=>setTimeout(resolve,remaining));steamQrClaimingShownAt=0;};
const refreshSteamQrAfterRejection=async(code)=>{
  stopSteamQrPolling();clearSteamQrCloseTimer();steamQrId=null;
  try{
    const r=await fetch(api('/api/qr/create'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})});
    const data=await r.json().catch(()=>({}));
    if(!r.ok){closeSteamQrModal();showResultModal(data.message||'二维码刷新失败，请重新提交。');return;}
    steamQrId=data.qr_id;
    showSteamQrModal(data.qr_svg,'Steam 手机端已拒绝上一次登录，二维码已刷新，请重新扫码确认。');
    steamQrTimer=setInterval(querySteamQr,2500);
    querySteamQr();
  }catch{closeSteamQrModal();showResultModal('二维码刷新失败，请重新提交。');}
};
const querySteamQr=async()=>{if(!steamQrId){queryActivationCode($('#qr-code').value.trim());return}if(steamQrInFlight)return;steamQrInFlight=true;try{const r=await fetch(api(`/api/qr/${encodeURIComponent(steamQrId)}/status`)),data=await r.json();if(r.status===410){stopSteamQrPolling();steamQrId=null;clearSteamQrCloseTimer();closeSteamQrModal();showResultModal(data.message||'二维码已过期，请重新生成。');return}if(!r.ok&&r.status!==202){if(r.status===409&&/拒绝登录|阻止了本次登录/.test(data.message||'')){await refreshSteamQrAfterRejection($('#qr-code').value.trim());return}if(![429,502,503,504].includes(r.status)){stopSteamQrPolling();steamQrId=null;clearSteamQrCloseTimer();closeSteamQrModal();showResultModal(data.message||'扫码状态查询失败。');return}setSteamQrMessage(data.message||'扫码状态查询失败，正在重试…');return}setSteamQrMessage(data.message||'等待 Steam 手机端扫码确认…');if(data.status==='confirmed'||data.status==='processing'){clearSteamQrCloseTimer();closeSteamQrModal();setRedemptionLoading(true,'正在登录...');}else if(data.status==='claiming'){clearSteamQrCloseTimer();closeSteamQrModal();if(!steamQrClaimingShownAt)steamQrClaimingShownAt=Date.now();setRedemptionLoading(true,'正在提货...');}else if(data.status==='completed'){await ensureSteamQrClaimingVisible();stopSteamQrPolling();steamQrId=null;clearSteamQrCloseTimer();setRedemptionLoading(false);closeSteamQrModal();showResultModal(data.message||'提交成功，请重启大厅。');}else if(data.status==='submitted'||data.status==='conflict'||data.status==='failed'){await ensureSteamQrClaimingVisible();stopSteamQrPolling();steamQrId=null;clearSteamQrCloseTimer();setRedemptionLoading(false);closeSteamQrModal();showResultModal(data.message||'领取状态已结束。');}}catch{setSteamQrMessage('扫码状态查询失败，正在重试…');}finally{steamQrInFlight=false;}};
$('#generate-qr').addEventListener('click',async()=>{const code=$('#qr-code').value.trim(),button=$('#generate-qr');if(!code){showResultModal('请输入激活码。');return}stopSteamQrPolling();clearSteamQrCloseTimer();steamQrId=null;closeSteamQrModal();button.disabled=true;setRedemptionLoading(true,'正在提货...');try{const r=await fetch(api('/api/qr/create'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})}),data=await r.json();if(!r.ok){showResultModal(data.message||'二维码创建失败。');return}steamQrId=data.qr_id;showSteamQrModal(data.qr_svg);steamQrTimer=setInterval(querySteamQr,2500);querySteamQr();}catch{showResultModal('无法连接提货服务，请稍后重试。');}finally{button.disabled=false;setRedemptionLoading(false);}});
$('#query-qr').addEventListener('click',querySteamQr);
$('#steam-qr-close').addEventListener('click',closeSteamQrWithGrace);$('#steam-qr-modal-confirm').addEventListener('click',closeSteamQrWithGrace);$('#steam-qr-modal').addEventListener('click',e=>{if(e.target===e.currentTarget)closeSteamQrWithGrace();});
